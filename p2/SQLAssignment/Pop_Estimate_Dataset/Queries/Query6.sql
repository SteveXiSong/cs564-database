SELECT 
	T.STNAME, SUM(A.POPESTIMATE2011)
FROM 
	POP_ESTIMATE_STATE_AGE_SEX_RACE_ORIGIN AS A 
	JOIN
	HOUSING_UNITS_STATE_LEVEL as T
WHERE 	
	A.SEX = 2 
	AND A.AGE>=21 AND A.AGE <= 45 
	AND A.STATE = T.STATE
GROUP BY A.STATE
;

